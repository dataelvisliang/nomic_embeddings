# requirements.txt
# Requirements for TripAdvisor Review Embedding Atlas Pipeline

# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Step 1: Embedding Generation (Qwen3-Embedding-4B)
torch>=2.0.0
transformers>=4.51.0
sentence-transformers>=2.7.0
accelerate>=0.20.0

# Step 2: Dimensionality Reduction & Quality Metrics
umap-learn>=0.5.3
scikit-learn>=1.3.0
scipy>=1.11.0
pyarrow>=12.0.0

# Step 3: Visualization (Streamlit + Embedding Atlas)
streamlit>=1.28.0
embedding-atlas>=0.1.0
duckdb>=0.9.0

# Optional: GPU acceleration (uncomment if using CUDA)
# torch==2.0.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html
